// This registers the custom keyboard module, it must be called from the root because the native module creates new root views
import "./configureKeyboard";
// This is the default entry point for the app with expo router, it will be used by the native side to load the app
import "expo-router/entry";
